# REFACTORED: Added comments for clarity and production readiness.

# Server Configuration
server.port=8080

# --- DATABASE CONFIGURATION (LOCAL) ---
# For production, it's recommended to use environment variables for credentials.
spring.datasource.url=jdbc:mysql://localhost:3306/ej?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Thor2611@@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA & HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- THYMELEAF VIEW TEMPLATES ---
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# --- CUSTOM APPLICATION PROPERTIES ---
# Secret key for the one-time admin registration endpoint.
admin.secret.key=ASGARD